# Run Output Directory

Each generated theme run should create a folder here named after the slug, for example `000_nolan_young_theme_premium_landscape_design_company/`.

The workflow writes plans, raw model output, compact Ollama site specifications, normalized renderer specs, validation output, and run metadata here so failures can be debugged after the fact.

