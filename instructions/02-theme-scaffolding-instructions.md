# Theme Scaffolding Instructions

- Preserve the required classic WordPress theme structure.
- Create all required files and folders, even when some are small.
- Use `contracts/required-theme-structure.md` as the minimum generated theme structure.
- Extra files are allowed when they belong in the correct theme folder and support the selected prompt.
- Keep the output installable as a standard theme ZIP.
- Do not omit required structural files.
