# Customization

- Edit content in the page templates or WordPress pages assigned to them.
- Update colors, spacing, and typography in `src/scss/`.
- Update interaction behavior in `src/js/main.js`.
- Replace local illustrations in `assets/images/`.
