#!/usr/bin/env bash
# Compatibility shim. Canonical shared functions live in scripts/repo/theme-factory-shared-functions.sh.
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/repo/theme-factory-shared-functions.sh"