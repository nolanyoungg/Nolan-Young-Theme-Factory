# Codex Full Generation Pass

Use this only for Codex-only mode, when no Ollama draft exists.

- Create the complete generated theme directly in the repository.
- Create the matching static preview directly in the repository.
- Treat the selected prompt file as the creative brief.
- Produce all required WordPress theme files, source files, compiled assets, local images, docs, and supporting files.
- Produce all required static preview pages, preview CSS, preview JS, preview README, and local preview images.
- Update `docs/index.html` with the generated preview card.
- Implement the Nolan-menu header system in the WordPress theme and static preview.
- Use local, copyright-safe raster images that match the business category.
- Do not use remote assets, CDNs, placeholder copy, sample services, lorem ipsum, or unfinished preview blocks.
- Do not behave like a finalization pass; there is no existing draft to preserve.
